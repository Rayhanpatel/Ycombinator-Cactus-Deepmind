Drop your unzipped Gemma model folder here as a folder reference for local testing.

Preferred first model:
- `gemma-4-e2b-it-int4-apple`

The app will look in:
- `Bundle.main/Models/<model-folder>`
- `Library/Application Support/Models/<model-folder>`

Do not flatten or rename files inside the model directory after unzip.
